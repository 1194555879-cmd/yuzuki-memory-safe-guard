# 柚月记忆安全护栏 v0.1.1

给 TauriTavern / SillyTavern 使用的独立安全扩展。

## 作用

- 强制关闭柚月的「隐藏楼层」。
- 强制关闭柚月的「总结后隐藏原楼层」。
- 覆盖并拦截 `YuzukiMemory.FloorHider` 的所有隐藏入口。
- 不修改剧情总结、填表、向量、世界书、记忆注入。
- 不接触 API、模型、密钥和预设。
- 不自动更新，避免静默改变行为。

## 重要限制

- 只负责阻止新的隐藏行为。
- 不自动修复已经损坏的 JSONL。
- 第一轮必须在聊天副本中测试。
